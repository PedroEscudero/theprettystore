<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-6">
    <div class="thumbnail">
        <%= image_tag @listado.image.url%>
    </div>
  </div>
      <div class="col-md-6">
        <h3><%= @listado.name %></h3>
        <p><%= number_to_currency(@listado.price, :unit => " € ") %></p>
        <p><%= @listado.description %></p>
  </div>
</div>



<%= link_to 'Edit', edit_listado_path(@listado), class:'btn btn-link' %> |
<%= link_to 'Back', listados_path, class:'btn btn-link' %>
